<template>
    <el-dialog 
    {{{if title}}} title="{{{title}}}" {{{/if}}}  
    @close="@@close"
    :visible.sync="@@show">
    {{{insertChildren()}}}
    </el-dialog>
</template>
<script>
export default {
  data() {
    return {
      show:false
    };
  },
  created(){
  },
  methods:{
      open(){
        this.@@show = true;
        {{{if loadComponent.value}}}
          this.{{{external(loadComponent)}}}()
        {{{/if}}}
      },
      close(){
        {{{if clearComponent.value}}}
          this.{{{external(clearComponent)}}}()
        {{{/if}}}
      }
  },
};
</script>
<style>
</style>

